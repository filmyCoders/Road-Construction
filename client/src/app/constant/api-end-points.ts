import { environment } from "../environments/environments";

export const GET_RECORDS: string = `${environment.baseApiUrl}Data/get-records`;
export const UPDATE_RECORDS: string = `${environment.baseApiUrl}Data/update-records`;